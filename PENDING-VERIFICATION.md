# 待驗證清單(下次接手先看這份)

這份跨兩個 skill(`deepseek-manual` + `deepseek-outsource`),列出目前還沒實際跑過的動作。目的是讓你或 CC 下次回來這個主題時,不用重新回想「上次做到哪」,直接照這份清單接續。

## deepseek-manual(dsh UI 操作手冊)未驗證的 6 項

1. `/compact` 實際觸發後的畫面(沒點過,不確定會不會消耗 API 額度)
2. `/feedback` 實際觸發後的互動介面長怎樣(沒點過)
3. 插件列表分頁的完整插件清單(只看過分類:終端/Agent 循環/網頁搜索,沒逐一展開列出品項)
4. 「上下文已用 X%」按鈕點下去有沒有額外資訊(只確認按鈕存在)
5. 「後台任務」面板在**還在執行中**的 session 上長什麼樣子(只看過已完工 session 的空清單)
6. 網頁 UI 有沒有 CLI 打不到的專屬能力,或反過來(沒驗證過)

補完一項,就去 `SKILL.md` 的「涵蓋率與未驗證清單」把那條刪掉、內容搬進對應章節,這份清單也同步刪掉那條。

## deepseek-outsource(委派工作流程)未驗證的部分

- **整套 14 步 SOP 一次都還沒真的拿任務跑過**(MVP 1.0 設計完成,MVP 2.0=首次實跑驗證,狀態:尚未開始)
- 四種任務類型雖然都有「今晚真實案例」佐證,但那些案例是在**舊的手動操作流程**下產生的,不是用這個新 skill 本身跑出來的——skill 化之後的具體步驟(尤其 browserclaw 操作細節、AskUserQuestion 觸發時機)還沒有被真實驗證過
- 尖峰時段檢查邏輯(命中 09-12/14-18 跳出選項問)寫好了,但沒有真的在尖峰時段跑過一次,不確定 AskUserQuestion 出現的時機/文案是否符合預期
- `/goal` 有動作輪數上限(看過 0/256)這件事,對於「自查清單」到底會吃掉多少輪數,沒有實測過具體數字

## 建議下次接手的做法

這兩份未驗證清單有重疊的操作成本(都需要開 browserclaw、進 dsh session),建議合併成一次會話處理:

1. 挑一個小任務,走一次 `deepseek-outsource` 完整 14 步(驗證 MVP 2.0)
2. 同一個 browserclaw session 裡,順手點開 `deepseek-manual` 沒探過的 6 項(尤其 `/compact`、`/feedback`、後台任務面板在執行中的樣子——這些在一次真實委派任務進行中最容易自然碰到)
3. 兩邊各自更新對應文件的狀態、commit、push
